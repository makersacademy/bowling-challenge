{
    "extends": "airbnb-base",

    /// This one prevents ESLint from running in JS files within src directory
    // "extends": "plugin:jasmine/recommended",

    "plugins": [
        "jasmine"
    ],

    "env": {
        "jasmine": true
    }
}
