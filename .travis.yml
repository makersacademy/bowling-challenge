language: node_js
node_js:
    - "0.10"
services:
  - xvfb
addons:
    chrome: stable
before_install:
  - npm i -g npm@6

# install:
#   - npm set progress=false
#   - npm install
script: node_modules/karma/bin/karma start my.conf.js --single-run
