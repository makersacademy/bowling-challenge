const Frame = require('./frame');
const Score = require('./score');
const App = require('./app');

describe('App', () => {
  it('promts the user to enter the score', () => {
    const app = new App();
    app.run

    // let aStub = sinon.stub('prompt');
    // aStub.withArgs('Please enter the first number of knocked down pins: ').returns(4);

    // const mockPrompt = { prompt: (message) => {
    //   message('Please enter the first number of knocked down pins: ');
    // }};
    
  });
});