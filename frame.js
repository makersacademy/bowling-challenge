class Frame {
  constructor() {
    this.score = 0;
  }

  countRolls() {
    let rollcount = 1;

    while (rollcount < 3) {
    //kanei pragmata
    score += rollscore
    rollcount += 1
    }
  }

  addRollScore(rollscore) {
    this.score += rollscore;
  }
}

module.exports = Frame;