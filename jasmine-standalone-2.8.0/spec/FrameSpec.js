'use strict';

describe("Frame", function() {

  var frame;

  beforeEach(function() {
    frame = new Frame();
  })

  // describe("bowl", function() {
  //   it("should add the score bowled to the frame's score", function() {
  //     frame.bowl(6);
  //     expect(frame.score).toEqual(6);
  //   })
  // })
  //
  // describe("Final frame", function() {
  //   it("should be set to the final frame when new Frame is passed true", {
  //     finalFrame = new Frame(true)
  //     expect(finalFrame.isFinalFrame).toEqual(true)
  //   })
  // })

})
