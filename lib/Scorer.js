function Scorer() {
  this.rolls = ({
    one: {
      first: null,
      second: null
    }
  })

  // Scorer.prototype.score()
}
// Player.prototype.play = function(song) {
//   this.currentlyPlayingSong = song;
//   this.isPlaying = true;
// };
