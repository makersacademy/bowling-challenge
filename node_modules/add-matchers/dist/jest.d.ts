/// <reference types="jest" />
import { CustomMatcher } from '.';
export declare type JestMatcherAdapter = (name: string, matcher: CustomMatcher) => jest.CustomMatcher;
export declare const getJestAdapter: (scope: {
    expect: jest.Expect;
}) => (name: string, matcher: CustomMatcher) => {
    [x: string]: jest.CustomMatcher;
};
