export declare type ToBeWithinRange = (floor: number, ceiling: number, expectationFailOutput?: any) => boolean;
declare global {
    namespace jasmine {
        interface Matchers<T> {
            toBeWithinRange: ToBeWithinRange;
        }
    }
}
export declare const toBeWithinRange: ToBeWithinRange;
