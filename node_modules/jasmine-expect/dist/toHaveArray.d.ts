export declare type ToHaveArray = (key: string, expectationFailOutput?: any) => boolean;
declare global {
    namespace jasmine {
        interface Matchers<T> {
            toHaveArray: ToHaveArray;
        }
    }
}
export declare const toHaveArray: ToHaveArray;
