export declare type ToHaveDate = (key: string, expectationFailOutput?: any) => boolean;
declare global {
    namespace jasmine {
        interface Matchers<T> {
            toHaveDate: ToHaveDate;
        }
    }
}
export declare const toHaveDate: ToHaveDate;
