export declare type ToHaveFalse = (key: string, expectationFailOutput?: any) => boolean;
declare global {
    namespace jasmine {
        interface Matchers<T> {
            toHaveFalse: ToHaveFalse;
        }
    }
}
export declare const toHaveFalse: ToHaveFalse;
