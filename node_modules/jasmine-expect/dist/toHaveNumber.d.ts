export declare type ToHaveNumber = (key: string, expectationFailOutput?: any) => boolean;
declare global {
    namespace jasmine {
        interface Matchers<T> {
            toHaveNumber: ToHaveNumber;
        }
    }
}
export declare const toHaveNumber: ToHaveNumber;
