/* As a player
to to start the game
I would like to play the game and get a score */

describe('Feature tests', function() {
  var game;
  // var scorecard;

  beforeEach(function() {
    game = new Game();
    // score = new Score();
    // scorecard = new Scorecard();
  });


});
