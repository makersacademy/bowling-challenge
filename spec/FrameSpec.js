describe("Frame", function() {

  beforeEach(function() {
    frame = new Frame;
  });
  // 
  // it("is defined", function() {
  //   expect(frame).toBeDefined();
  // });
  //
  // it("stores the number of knocked down pins", function() {
  //   expect(frame.pins).toBeDefined();
  // });
  //
  // it("remembers the number of pins knocked down after every roll", function() {
  //   frame.roll(5);
  //   expect(frame.pins).toEqual(5);
  // });

});
