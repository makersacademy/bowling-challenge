describe('Game', function() {
  beforeEach(function() {
    game = new Game();
  })

  

  // it('starts with empty frames array,', function() {
  //   expect(game._frames).toEqual([])
  // })
  //
  // it('a score of zero,', function() {
  //   expect(game.showScore()).toEqual(0)
  // })
  //
  // it('and with a new, empty current frame.', function() {
  //   expect(game._currentFrame._rolls).toEqual([])
  // })
  //
  // it('It can add pins to score', function() {
  //   game.addToScore(5)
  //   game.addToScore(2)
  //   expect(game.showScore()).toEqual(7)
  // })
  //
  // it('across multiple frames', function() {
  //   game.addToScore(2)
  //   game.addToScore(3)
  //   game.addToScore(4)
  //   expect(game.showScore()).toEqual(9)
  //   expect(game._frames.length).toEqual(1)
  // })
  //
  // it('up to a maximum of ten', function() {
  //   game.addToScore(2)
  //   game.addToScore(3)
  //   game.addToScore(4)
  //   expect(game.showScore()).toEqual(9)
  //   expect(game._frames.length).toEqual(1)
  // })
})
