'use strict';

describe('Feature Test', function() {

  var game;
  var score;

  beforeEach(function() {
    game = new Game();
    score = new Score();
  });

  it('', function() {
    expect().to(0);
  });
});
