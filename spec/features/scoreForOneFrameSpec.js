

  describe('Score for one frame',function() {

    var frame;

    beforeEach(function() {
      frame = new Frame();
    });

    describe('record score for roll one',function() {
      // it('open - after one frame and one roll',function(){
      //   frame[0].rollPins(0,5);
      //   frame[0].rollPins(0,7);
      //   expect(frame[0].frameScore()).toEqual(12);
      // });


    });

  });

