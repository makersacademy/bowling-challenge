describe("Frame", function() {
  var frame;

  beforeEach(function() {
    frame = new Frame();
  });

  // describe("Calculating a frame's score", function() {
  //   it("adds up the frame score", function() {
  //     frame.calculateTotal(2, 4);
  //     expect(frame.currentRoll()).toEqual(6);
  //   })
  // });
});
