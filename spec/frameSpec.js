describe("Frame", function () {
    var frame;

    beforeEach(function () {
        frame = new Frame();
    });

    // it('can create a frame', function () {
    //   expect(frame.roll())
    // });

});