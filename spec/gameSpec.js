/* eslint-env jasmine */

describe('Game', () => {
  let game;

  beforeEach(() => {
    game = new Game();
  });

  describe('Initialise', () => {
    it('initialises with 10 frames'){
       game.
    }
  });
})
