
function Game() {

  this.rolls=1;
  this.pins=[]; //points in each frame [5,5]
  this.totalScore=0;


  this.roll = function(amountOfHitPins) {

  };

  this.score= function(){
    return 0
  }
  /*
  this.gutterGame = function() {
    if (this.pins===[] && this.roll()===20){
    };
    return this.totalScore= 0
  };

  this.perfectGame=function() {
    if (this.roll() === 12 && this.pins[120] ){
      return this.totalScore=300
    };
    return this.totalScore = 300
  };

  this.pointsInOneFrame = function() {
    var points1=0;// this will be wrapped in jQuery
    var points2=1;// same as above
     this.pins.push(points1, points2)
     this.allScores.push(this.pins)

     return this.allScores
  };
console.log(pointsInOneFrame);
  this.sumUpFrameInFirstRound=function() {
    this.sum=this.pointsInOneFrame()[0]+ this.pointsInOneFrame()[1];
    return this.sum
  };
  // this.sumUpPointsFrames2_9=function() {
  //   if (this.pins.includes(10){
  //     frame+1 then this.pins[0] add + frame-1
  //   })
  //   this.frameSum=this.sumUpFrameInFirstRound+
  // }



  //
  // this.score = function() {
  //    for (var frame=0; frame<=10; frame++) {
  //             this.allScores += "f "+frame;
  //   };
  // };

*/

}
