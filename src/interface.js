'use strict';

$(document).ready(function () {

  var bowlingGame = new BowlingGame();
  game.addFrame(new Frame());


  ballThrows.forEach(function (ballThrow) {
    frame.addFrame(ballThrow);
  });

})
