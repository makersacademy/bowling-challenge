$(document).ready(() => {
  const game = new Game();

  $('#bowl').click(() => {
    const pins = parseInt($('#pinScore').val());
    updateBowlScore(pins);
  });

  function updateBowlScore(pins) {
    $();
  }
});
