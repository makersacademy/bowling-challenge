var test = new Scorecard()

frame1 = new Frame

frame0 = new Frame

frame2 = new Frame
frame0.bowl1(10);

frame1.bowl1(10);

frame2.bowl1(1);

frame2.bowl2(3);

test.addFrame(frame0);
test.calcTots();

test.addFrame(frame1);
test.calcTots();

test.addFrame(frame2);
test.calcTots();

var frame3 = new Frame;
frame3.bowl1(9);
frame3.bowl2(1);

test.addFrame(frame3)

test.calcTots();

test
